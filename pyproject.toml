[project]
name = "discord-music-bot"
version = "0.1.0"
description = "A small, private Discord music bot that streams from YouTube."
requires-python = ">=3.11"
dependencies = [
    "discord.py[voice]>=2.4.0",
    "yt-dlp>=2024.1.0",
    "python-dotenv>=1.0.0",
    "PyNaCl>=1.5.0",
]
